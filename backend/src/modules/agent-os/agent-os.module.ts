/**
 * Agent OS Module (Placeholder)
 *
 * Future home for Agent runtime environment capabilities:
 * - Agent lifecycle management
 * - Inter-agent communication bus
 * - Agent sandboxing and resource limits
 * - Agent marketplace and distribution
 *
 * Currently empty — will be populated when Agent OS features are implemented.
 * Not registered in AppModule until it has actual providers.
 */

import { Module } from "@nestjs/common";

@Module({})
export class AgentOSModule {}
