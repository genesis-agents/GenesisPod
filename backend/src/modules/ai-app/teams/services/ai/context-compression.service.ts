/**
 * Context Compression Service
 *
 * 大上下文压缩服务
 *
 * ⚠️ 已迁移到 AI Engine 核心能力层
 * 此文件保留为重导出，保持向后兼容
 *
 * @see ai-engine/planning/services/context-compression.service.ts
 */

// 从 AI Engine 重导出（向后兼容）
export {
  ContextCompressionService,
  type DataChunk,
  type SummaryChunk,
  type CompressionResult,
  type CompressionOptions,
} from "../../../../ai-engine/facade";
